<!DOCTYPE html><html lang="en"><head><x-page-head title="Create account"/></head><body class="auth-page"><main class="auth-shell"><a class="auth-brand" href="/"><img src="{{ asset('brand/afterfeed-mark.svg') }}" alt="">Afterfeed</a><section class="auth-card"><p class="eyebrow">YOUR PRIVATE ARCHIVE</p><h1>Create your account</h1><p>The first account will adopt any archives already stored in this installation.</p><form method="post" action="{{ route('register.store') }}">@csrf<label>Name<input name="name" value="{{ old('name') }}" required autofocus autocomplete="name"></label><label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label><label>Password<input type="password" name="password" required autocomplete="new-password"></label><label>Confirm password<input type="password" name="password_confirmation" required autocomplete="new-password"></label>@if($errors->any())<div class="auth-error">{{ $errors->first() }}</div>@endif<button>Create account</button></form><small>Already have an account? <a href="{{ route('login') }}">Sign in</a></small></section></main></body></html>
